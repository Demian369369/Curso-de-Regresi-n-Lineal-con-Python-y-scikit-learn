----->Análisis de correlación de los datos<---- σ 1ero es hacer el analisis de correlaciones. Para ello seguiremos casi los mismo pasos del ejercicio anteior.


1. " import matplotlib.pyplot as plt "
     +Ok, todo bien.
" sns.pairplot(df, height=2.5) " +Esto debido a que la 1era grafica que se uso, fue el sns pairplot. " plt.show() " +Ya sabemos que esto es para qye nos muestre la grafica. 2. " import numpy as np " " numeric_cols = ['age', 'bmi', 'children', 'charges'] " " cm = np.corrcoef(df[numeric_cols].values.T) " " sns.set(font_scale=1.5) " "sns.heatmap(cm,annot=True,yticklabels=numeric_cols,xticklabels=numeric_cols) +Esto es para ya poner el mapa de calor, +En donde tamb se le pide que haga anotaciones ( "cn,annot=true" ), ----------->Ya que estamos usando arreglos de numpy, este no nos muestra los encabezados o nada que no sea numerico. + Por ello, se utiliza yticklabels =numeric_cols,xticklabels=numeric_cols), en donde basicamente se le esta diciendo que las etiquetas de de las y-axis tengan el valor de los numeric_cols. Lo mismo con los xticklabels. ----------->Una vez listo, empezaremos a hacer un " One hot en coding ". Profe Luis explica un ejemplo en donde nuestra base de datos, incluyen si nuestro paciente es o no fumador (en formato de casillas), esto nos ayudara a coding seria como que cuando el paciente sea fumador = 1 y cuando no = 0. σ Para ello, Panda ya tiene una funcion la cual es. "df =pd.get_dummies(df,columns=['sex','smoker','region'], drop_first=True) " " df.head() " + Al parecer la funcion es "get_dummies " y seleccionamos las variables a usar. + Y al ver que se veia mal que, por ejemplo, un paciente que tiene muchos " 0s " pero solo un " 1 ", es mejor si quitamos esos "0s" y dejamos en lo que si tiene valor. Para ello, se usa " drop_first=True " +Por ultimo, usamos " df.head() ", la cual se usa para obtener las primeras n filas.

